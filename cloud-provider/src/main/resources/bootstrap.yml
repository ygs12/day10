spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
    zookeeper:
      connect-string: 192.168.3.201:2182
      discovery:
        register: true
  application:
    name: provider


